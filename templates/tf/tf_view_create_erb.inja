<!DOCTYPE html>
<%#include "{{ lower(lCamel(create table)) }}.h" %>
<% tfetch(QVariantMap, {{ lCamel(create table) }}); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>New {{ uSpace(create table) }}</h1>

<%== formTag(urla("create"), Tf::Post) %>
## for column in columns
## if not mapBool("exclude", column/name)
## if not existsIn(column, "serial") and column/serial
## if existsIn(column, "type") and not column/type == "serial"

  <p>
    <label>{{ uSpace(column/name) }}<br /><input name="{{ lCamel(create table) }}[{{ lCamel(column/name) }}]" value="<%= {{ lCamel(create table) }}["{{ lCamel(column/name) }}"] %>" /></label>
  </p>
## endif
## endif
## endif
## endfor

  <p>
    <input type="submit" value="Create" />
  </p>
</form>

<%== linkTo("Back", urla("index")) %>

</body>
</html>
