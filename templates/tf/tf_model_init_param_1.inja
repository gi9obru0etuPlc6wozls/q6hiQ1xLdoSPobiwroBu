## if map("qt_type", column/type) == "int"

    d->{{column/name}} = 0;
## endif
## if map("qt_type", column/type) == "integer"

    d->{{column/name}} = 0;
## endif
## if map("qt_type", column/type) == "float"

   d->{{column/name}} = 0;
## endif
## if map("qt_type", column/type) == "double"

    d->{{column/name}} = 0;
## endif
## if map("qt_type", column/type) == "numeric"
    d->{{column/name}} = 0;
## endif