## for column in columns
## if existsIn(column, "primary_key")
## if column/primary_key
## for column2 in columns
## if column2/name == "lock_revision"

{{ uCamel(create_table) }} {{ uCamel(create_table) }}::get({% include "tf_model_create_param.inja" %}, {{ map("qt_type", column2/type) }} lockRevision)
{
    TSqlORMapper<{{ uCamel(create_table) }}Object> mapper;
    TCriteria cri;
    cri.add({{ uCamel(create_table) }}Object::{{ uCamel(column/name) }}, {{ lCamel(column/name) }});
    cri.add({{ uCamel(create_table) }}Object::{{ uCamel(column2/name) }}, {{ lCamel(column2/name) }});
    return {{ uCamel(create_table) }}(mapper.findFirst(cri));
}
## endif
## endfor
## endif
## endif
## endfor