<!DOCTYPE html>
<%#include "{{ lower(lCamel(create_table)) }}.h" %>
<% tfetch({{ uCamel(create_table) }}, {{ lCamel(create_table) }}); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Showing Blog</h1>
## for column in columns
## if not mapCompare("exclude", column/name, true)
## if not compare(column, "serial", true)
## if existsIn(column, "type") and not column/type == "serial"

<dt>{{ uSpace(column/name) }}</dt><dd><%= {{ lCamel(create_table) }}.{{ lCamel(column/name) }}() %></dd><br />
## endif
## endif
## endif
## endfor

<%== linkTo("Edit", urla("save", {{ lCamel(create_table) }}.{% include "get_pk.inja" %}())) %> |
<%== linkTo("Back", urla("index")) %>

</body>
</html>
