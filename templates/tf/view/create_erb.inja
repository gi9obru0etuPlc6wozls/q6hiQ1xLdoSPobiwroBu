<!DOCTYPE html>
<%#include "{{ lCamel( create_table ) }}.h" %>
<% tfetch(QVariantMap, {{ lCamel( create_table ) }}); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>New {{ uSpace( create_table ) }}</h1>

<%== formTag(urla("create"), Tf::Post) %>
## for column in columns
## if not mapCompare("exclude", column/name, true)
## if not compare(column, "primary_key", true)
  <p>
    <label>{{ uSpace( column/name ) }}<br /><input name="{{ lCamel( create_table ) }}[{{ lCamel( column/name ) }}]" value="<%= {{ lCamel( create_table ) }}["{{ lCamel( column/name ) }}"] %>" /></label>
  </p>
## endif
## endif
## endfor
  <p>
    <input type="submit" value="Create" />
  </p>
</form>

<%== linkTo("Back", urla("index")) %>

</body>
</html>
